syntax = "proto3";
package api;

option go_package = ".;api";

message LogMessage {
  string body = 1;
}

service LogMessageService {
  rpc Send(LogMessage) returns (LogMessage) {}
}


/*
protoc -I src/basic/api -I${GOPATH}/src --go_out=plugins=grpc:src/basic/api src/basic/api/log.proto
*/